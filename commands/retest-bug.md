---
description: Retest a bug from Jira ticket — login, test, compare Swagger, draft Jira comment, transition ticket
---

Invoke the `retest-bug-workflow` skill to retest a bug.

Pass any arguments the user provided after `/retest-bug` as the Jira ticket link or key. If no arguments were provided, the skill will ask the user for the ticket link.
